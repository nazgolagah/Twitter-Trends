import { changeFormat } from 'twitter-trends/helpers/change-format';
import { module, test } from 'qunit';

module('Unit | Helper | change format');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = changeFormat([42]);
  assert.ok(result);
});
